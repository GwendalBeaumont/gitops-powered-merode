# GitOps-powered MERODE

## Cloning this repository

```sh
git clone --recurse-submodules -j8 https://github.com/GwendalBeaumont/gitops-powered-merode.git
```

## Requirements

- Container software (e.g., Docker, Podman, Orbstack)
- `docker-compose` or equivalent

## Starting the stack

Simply use this command:

```sh
docker compose up
```

Or in detached mode:

```sh
docker compose up -d
```

